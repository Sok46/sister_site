# 📝 Руководство по редактированию сайта

## Структура проекта

```
sister_site/
├── app/                    # Страницы сайта
│   ├── page.tsx           # Главная страница
│   ├── yoga/page.tsx      # Страница "Йога"
│   ├── nutrition/page.tsx # Страница "Питание"
│   ├── family/page.tsx    # Страница "Семья"
│   └── blog/             # Блог
│       ├── page.tsx       # Список постов
│       └── [id]/page.tsx  # Страница отдельного поста
├── components/            # Компоненты (блоки)
│   ├── Navigation.tsx     # Меню навигации
│   ├── Footer.tsx         # Подвал сайта
│   └── VideoPlayer.tsx   # Видеоплеер
├── content/posts/         # Посты блога (Markdown)
└── public/photos/         # Фотографии
```

---

## 🏠 Редактирование главной страницы

**Файл:** `app/page.tsx`

### Изменение заголовка

Найдите строку:
```tsx
<h1 className="text-5xl md:text-7xl font-serif font-bold text-gray-900 mb-6">
  Добро пожаловать в мир
  <span className="block text-primary-600 mt-2">Йоги и Гармонии</span>
</h1>
```

Измените текст внутри `<h1>` и `<span>`.

### Изменение описания

Найдите:
```tsx
<p className="text-xl md:text-2xl text-gray-700 mb-8 max-w-3xl mx-auto">
  Откройте для себя путь к здоровью...
</p>
```

Измените текст внутри `<p>`.

### Добавление/изменение карточек разделов

Найдите секцию с карточками (около строки 50-80):

```tsx
<div className="card p-8 text-center">
  <div className="text-5xl mb-4">🧘</div>
  <h3 className="text-2xl font-serif font-bold text-gray-900 mb-4">
    Йога
  </h3>
  <p className="text-gray-600 mb-6">
    Практики, асаны, медитации...
  </p>
  <Link href="/yoga" className="text-primary-600...">
    Узнать больше →
  </Link>
</div>
```

Скопируйте этот блок и измените:
- Эмодзи (`🧘`)
- Заголовок (`Йога`)
- Описание
- Ссылку (`href="/yoga"`)

### Изменение фотогалереи

Найдите секцию "Gallery Preview" (около строки 100):

```tsx
{['photo_2025-12-01_17-27-40.jpg', 'photo_2025-12-23_15-02-25.jpg', 
  'photo_2026-01-15_14-48-15.jpg', 'photo_2026-01-16_12-50-28.jpg'].map((photo, idx) => (
```

Измените массив с именами файлов на ваши фотографии из папки `public/photos/`.

---

## 📄 Редактирование страниц разделов

### Страница "Йога"
**Файл:** `app/yoga/page.tsx`

Измените текст в секциях:
- "О моей практике" - описание вашей практики йоги
- "Популярные практики" - список практик
- Добавьте новые секции, скопировав структуру существующих

### Страница "Питание"
**Файл:** `app/nutrition/page.tsx`

Аналогично странице йоги - редактируйте текст и добавляйте новые блоки.

### Страница "Семья"
**Файл:** `app/family/page.tsx`

Редактируйте информацию о семье и воспитании.

---

## 🧩 Редактирование компонентов (блоков)

### Меню навигации
**Файл:** `components/Navigation.tsx`

Измените название сайта:
```tsx
<span className="text-2xl font-serif font-bold text-primary-600">
  🌸 Йога & Жизнь
</span>
```

Добавьте/удалите пункты меню:
```tsx
const navItems = [
  { href: '/', label: 'Главная' },
  { href: '/yoga', label: 'Йога' },
  // Добавьте новый пункт:
  { href: '/new-page', label: 'Новая страница' },
]
```

### Подвал сайта
**Файл:** `components/Footer.tsx`

Измените информацию в футере, контакты, ссылки.

---

## 📝 Добавление новых постов в блог

**Папка:** `content/posts/`

1. Создайте новый файл с расширением `.md`, например: `my-post.md`

2. Добавьте структуру:
```markdown
---
title: "Название вашего поста"
date: "2026-01-25"
category: "Йога"
excerpt: "Краткое описание поста"
emoji: "🧘"
image: "/photos/photo_2026-01-15_14-48-15.jpg"
video: "https://www.youtube.com/watch?v=VIDEO_ID"
---

# Заголовок поста

Ваш текст здесь. Используйте **жирный** и *курсив*.

## Подзаголовок

- Список
- Элементов

![Описание](/photos/photo.jpg)
```

3. Сохраните файл - пост появится автоматически на странице блога!

---

## 🎨 Изменение цветов и стилей

### Основные цвета
**Файл:** `tailwind.config.js`

Найдите секцию `colors`:
```js
colors: {
  primary: {
    50: '#fef7f0',
    500: '#ee7a4a',  // Основной цвет
    600: '#df5c2a',
  },
  accent: {
    500: '#36966a',  // Акцентный цвет
  },
}
```

Измените значения цветов (hex коды).

### Глобальные стили
**Файл:** `app/globals.css`

Добавьте свои стили в конец файла.

---

## ➕ Добавление новой страницы

### Шаг 1: Создайте папку и файл

Создайте папку в `app/`, например `app/about/` и файл `page.tsx`:

```tsx
export default function AboutPage() {
  return (
    <div className="section-padding">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-5xl font-serif font-bold text-center text-gray-900 mb-4">
          О нас
        </h1>
        <p className="text-xl text-center text-gray-600 mb-12">
          Информация о блоге
        </p>
        {/* Ваш контент */}
      </div>
    </div>
  )
}
```

### Шаг 2: Добавьте в меню

В файле `components/Navigation.tsx` добавьте:
```tsx
{ href: '/about', label: 'О нас' },
```

---

## 🖼️ Добавление фотографий

1. Поместите фото в папку `public/photos/`
2. Используйте в коде:
```tsx
<Image
  src="/photos/имя_файла.jpg"
  alt="Описание"
  width={800}
  height={600}
/>
```

Или в markdown постах:
```markdown
![Описание](/photos/имя_файла.jpg)
```

---

## 🎥 Добавление видео

### В компоненте:
```tsx
import VideoPlayer from '@/components/VideoPlayer'

<VideoPlayer 
  src="https://www.youtube.com/watch?v=VIDEO_ID" 
  title="Название видео"
/>
```

### В markdown посте:
Добавьте в frontmatter:
```markdown
---
video: "https://www.youtube.com/watch?v=VIDEO_ID"
---
```

## 📱 Добавление постов из Telegram

### В компоненте:
```tsx
import TelegramEmbed from '@/components/TelegramEmbed'

<TelegramEmbed url="https://t.me/Zoya_Sergienko/780" />
```

### В markdown посте:
Добавьте в frontmatter:
```markdown
---
telegram: "https://t.me/Zoya_Sergienko/780"
---
```

Пост из Telegram автоматически встроится на страницу! Подробнее см. `TELEGRAM_INTEGRATION.md`

---

## 🔄 Как применить изменения

1. Сохраните файл (Ctrl + S)
2. Если `npm run dev` запущен, изменения применятся автоматически
3. Обновите страницу в браузере (F5)

---

## 💡 Полезные советы

### Классы Tailwind CSS

- `text-4xl` - размер текста
- `font-bold` - жирный текст
- `text-primary-600` - цвет текста
- `mb-4` - отступ снизу
- `card` - стиль карточки (уже настроен)
- `btn-primary` - стиль кнопки (уже настроен)

### Структура блока

```tsx
<section className="section-padding bg-white">
  <div className="max-w-7xl mx-auto">
    <h2 className="text-4xl font-serif font-bold text-center mb-12">
      Заголовок
    </h2>
    <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
      {/* Контент */}
    </div>
  </div>
</section>
```

---

## 📚 Дополнительные ресурсы

- [Документация Next.js](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Markdown Guide](https://www.markdownguide.org/)

---

## ❓ Частые вопросы

**Q: Как изменить шрифт?**
A: В `app/layout.tsx` измените импорты шрифтов из Google Fonts.

**Q: Как добавить новый раздел в меню?**
A: В `components/Navigation.tsx` добавьте элемент в массив `navItems`.

**Q: Как изменить размер карточек?**
A: Измените классы `grid-cols-1 md:grid-cols-3` на нужное количество колонок.

**Q: Где хранятся стили кнопок?**
A: В `app/globals.css` в секции `@layer components`.
