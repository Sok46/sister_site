# 📦 Установка Node.js

## Проблема
Ошибка `npm не распознан` означает, что Node.js не установлен на вашем компьютере.

## Решение: Установка Node.js

### Шаг 1: Скачайте Node.js

1. Откройте браузер
2. Перейдите на сайт: **https://nodejs.org/**
3. Вы увидите две кнопки:
   - **LTS** (рекомендуется) - стабильная версия
   - **Current** - последняя версия

4. **Нажмите на кнопку LTS** (зелёная кнопка)
   - Это скачает установщик для Windows

### Шаг 2: Установите Node.js

1. Запустите скачанный файл (например, `node-v20.x.x-x64.msi`)
2. Следуйте инструкциям установщика:
   - Нажимайте "Next" (Далее)
   - Примите условия лицензии
   - Оставьте путь установки по умолчанию
   - **Важно:** Убедитесь, что стоит галочка "Automatically install the necessary tools" (если есть)
3. Нажмите "Install" (Установить)
4. Дождитесь завершения установки
5. Нажмите "Finish" (Готово)

### Шаг 3: Перезапустите PowerShell

**ОБЯЗАТЕЛЬНО:** Закройте текущее окно PowerShell и откройте новое!

1. Закройте PowerShell (закройте окно)
2. Откройте PowerShell снова:
   - Windows + X → Windows PowerShell
   - Или Windows → введите "powershell"

### Шаг 4: Проверьте установку

В новом окне PowerShell введите:

```powershell
node --version
```

Должно показать что-то вроде: `v20.11.0` (версия может отличаться)

Затем проверьте npm:

```powershell
npm --version
```

Должно показать что-то вроде: `10.2.4` (версия может отличаться)

### Шаг 5: Теперь установите зависимости проекта

```powershell
cd D:\qgis\sister_site
npm install
```

---

## Альтернативный способ: Через winget (если доступен)

Если у вас Windows 10/11 с winget, можно установить через командную строку:

```powershell
winget install OpenJS.NodeJS.LTS
```

Затем перезапустите PowerShell.

---

## Что делать после установки

После успешной установки Node.js:

1. ✅ Перезапустите PowerShell
2. ✅ Перейдите в папку проекта: `cd D:\qgis\sister_site`
3. ✅ Установите зависимости: `npm install`
4. ✅ Запустите сайт: `npm run dev`

---

## Если проблемы остаются

### Проблема: Команды всё ещё не работают после установки

**Решение:**
1. Полностью закройте все окна PowerShell
2. Перезагрузите компьютер
3. Откройте PowerShell снова
4. Попробуйте команды снова

### Проблема: Не могу скачать с сайта

**Альтернативные источники:**
- Microsoft Store: Найдите "Node.js" в Microsoft Store
- Chocolatey (если установлен): `choco install nodejs-lts`

---

## Проверка успешной установки

После установки и перезапуска PowerShell выполните:

```powershell
node --version
npm --version
```

Если обе команды показывают версии - всё готово! 🎉
